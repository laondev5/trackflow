# ---- Core ----
MONGODB_URI=mongodb://127.0.0.1:27017/taskflow
# Generate with: node -e "console.log(require('crypto').randomBytes(48).toString('base64url'))"
JWT_SECRET=change-me-to-a-long-random-string
APP_URL=http://localhost:3000

# ---- Email (Nodemailer / SMTP) ----
# Gmail: use smtp.gmail.com, port 465, SMTP_SECURE=true and an App Password
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=you@example.com
SMTP_PASS=your-app-password
EMAIL_FROM="TaskFlow <you@example.com>"

# ---- Reminders ----
# Reminders run automatically on the server after normal app requests — no cron needed.
# Optional: protects the manual trigger endpoint /api/cron/reminders ("Authorization: Bearer <CRON_SECRET>")
CRON_SECRET=another-long-random-string
# Optional (long-running servers only): also run reminders on a 60s timer. Ignored on Vercel.
ENABLE_INTERNAL_CRON=false

# ---- AI task planner ----
# Gemini is used first; Groq is the automatic fallback. Configure at least one.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash
GROQ_API_KEY=
GROQ_MODEL=openai/gpt-oss-120b

# ---- Web Push (optional) ----
# Generate with: npx web-push generate-vapid-keys
NEXT_PUBLIC_VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:you@example.com
